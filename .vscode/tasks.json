{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start firechannel",
            "type": "shell",
            "command": "firechannel.exe -i robocik",
            "options": {
                "cwd": "${workspaceFolder}/target/debug"
            },
            "problemMatcher": [
                "$eslint-stylish"
            ]
        }
    ]
}